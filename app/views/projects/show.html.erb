<p id="notice"><%= notice %></p>

<p>
  <strong>User:</strong>
  <%= @user.name %>
</p>

<p>
  <strong>Route:</strong>
  <%= link_to @route.name, @route %> (<%= link_to @grade.name, @grade %>)
</p>

<p>
	<strong>Info:</strong>
	<%= @route.info %>
</p>

<p>
	<strong>Status:</strong>
	<%= @project.status %>
</p>

<%= link_to 'Edit', edit_project_path(@project) %> |
<%= link_to 'Back', projects_path %>
